services:
  - type: web
    name: lamini-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    plan: free            # upgrade to starter/pro if you need more RAM
    autoDeploy: true
    region: oregon